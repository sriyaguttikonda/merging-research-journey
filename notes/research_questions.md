Cross-architecture merging without significant compute — can you merge a fine-tune from one model family (e.g., Llama) with a fine-tune from another (e.g., Qwen) while preserving accuracy? Open problem; existing methods need permutation alignment or distillation. Sub-question that might be tractable: merging across scales within a single family.
The "outlier features" idea could apply to task vectors too. Some delta parameters connect to outlier features; some don't. DARE drops randomly without considering activations. Could a "DARE + activation-weighted importance" approach do better than pure random dropping?
This is a real research question. As far as I know, nobody has cleanly combined Wanda-style activation-weighted importance with merging. There's a gap here that aligns with your interests.
Per-output pruning is a structural constraint that may matter for merging. TIES and DARE work globally on task vectors. What if you pruned task vectors per-output instead of globally? Would the structural constraint help with interference?
